import 'package:flutter/material.dart';

class FlashbacksScreen extends StatelessWidget {
  const FlashbacksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Flashbacks Screen')),
    );
  }
}
